interface StatusProps {
    status:string,
   
}
interface RoleProps{
    role:string,
}
export const UpdateStatus = async ({
    // userId,
    status}:StatusProps) => {
  
}

export const UpdateRole = async ({
    // userId,
    role}:RoleProps) => {
  
}